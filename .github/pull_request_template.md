## Summary

<!-- What does this PR do and why? -->

## Changes

-

## How was this tested?

- [ ] `npm run lint` / `npx tsc --noEmit` / `npm run build` pass locally
- [ ] `uv run ruff check .` passes locally
- [ ] Verified in the browser (if UI change)
